{
  "dependencies": {
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/ios": "^7.4.4"
  },
  "scripts": {
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:css:fix": "stylelint \"css/**/*.css\" --fix",
    "lint": "npm run lint:css",
    "build": "echo 'Build for Capacitor only - run locally'",
    "build:ios": "mkdir -p dist && cp index.html dist/ && cp -r js dist/ && cp -r css dist/ && cp -r artwork dist/ && cp config.js dist/ && echo 'Build complete!'"
  }
}